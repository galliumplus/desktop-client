<Page x:Class="Couche_IHM.Frames.FrameLogs"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Couche_IHM.Frames"
      mc:Ignorable="d" 
      d:DesignHeight="450" 
      d:DesignWidth="800"
      Title="FrameLogs">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*" />
            <RowDefinition Height="5*" />
        </Grid.RowDefinitions>

        <Label x:Name="titleLog" Content="Aucun log" FontSize="30"/>

        <!-- Liste des logs -->
        <DataGrid Name="listLogs" AutoGenerateColumns="False" Grid.Row="1" CanUserResizeColumns="False" CanUserResizeRows="False" CanUserReorderColumns="False" IsReadOnly="True" SelectionChanged="ShowRowDetails">
            <!-- Columns -->
            <DataGrid.Columns>
                <DataGridTextColumn Header="Date" Binding="{Binding Date}" Width="100" />
                <DataGridTextColumn Header="Action" Binding="{Binding Action}" Width="150"/>
                <DataGridTextColumn Header="Auteur" Binding="{Binding Auteur}" Width="auto" />
                <DataGridTextColumn Header="Mesage" Binding="{Binding MessageCourt}" Width="auto"/>
            </DataGrid.Columns>
            
            <!-- Détails des lignes-->
            <DataGrid.RowDetailsTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding MessageComplete}" Margin="10" />
                </DataTemplate>
            </DataGrid.RowDetailsTemplate>
            
        </DataGrid>
    </Grid>
</Page>
